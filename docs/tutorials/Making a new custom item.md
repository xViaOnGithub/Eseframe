# Tutorial: Making a custom item

Cores can add new recipes to Eseframe by using the command `function eseframe:add_item` during the function tag `#eseframe_api`. This command is usually called inside a function that runs when your datapack loads.

#todo