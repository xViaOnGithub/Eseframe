# Called AS and AT the player using an item, which contains the id and namespace of the custom item.

# Call function tag at <root of every datapack>/data/<core's namespace>/tags/function/eseframe_api/item/<custom item id>/on_consume.mcfunction
$function #$(namespace):eseframe_api/item/$(id)/consume
