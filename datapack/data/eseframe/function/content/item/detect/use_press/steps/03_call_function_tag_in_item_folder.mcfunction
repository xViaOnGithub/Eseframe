# Called AS and AT the player using an item, WITH the item's Eseframe custom data, which contains the id and namespace of the custom item.

# Call function at <root of every datapack>/data/<core's namespace>/tags/function/eseframe_api/item/<custom item id>/use_press.mcfunction
$function #$(namespace):eseframe_api/item/$(id)/use_press