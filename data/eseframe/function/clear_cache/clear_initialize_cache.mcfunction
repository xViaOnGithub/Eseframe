# This function is named differently than other functions in this directory to
#   make it clear that this function is NOT the cache being initialized, and that
#   if is instead clearing the cache used by initialization of other paths.

data remove storage eseframe:cache initialize